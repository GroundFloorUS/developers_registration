<div class="container">
	<%= render :partial => "comfy/navbar", locals: { title: "Confirm Your Account", step: @registration.step, path: @path } %>


  <div class="row">
    <div class="span8">
      <div class="box white">
				<% unless @user.id %>
        	<p class="message">Before you can create your profile you must confirm the following information.</p>
				<% else %>
					<p class="message"><b>You’re all set</b>. Please confirm your name and email.</p>
				<% end %>
				<%= display_flash_messages(flash) %>
				<%= render partial: "account", locals: { user: @user } %>
      </div>
    </div>
		<%= cms_snippet_content("developers--confirm-account") %>

	</div>
	
</div>

<%= cms_snippet_content("footer") %>
